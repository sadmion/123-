# 版本记录

本项目采用语义化版本号，每次发布打 tag（`vX.Y.Z`），CI 自动构建镜像到 GHCR。

- 镜像地址：`ghcr.io/sadmion/pan123-library`
- 构建记录：https://github.com/sadmion/123-/actions

---

## v1.0.6 — 2026-09-16

### 改进

- **分类浏览区支持上下滚动** —— 折叠态 236px、展开态 70vh，两种状态都可滚动；
  新增细滚动条（8px 圆角，hover 变主题色），滚到底不带动整页
- **右侧面板宽度可拖动** —— 拖动左边缘分隔条调整宽度（240~720px），
  双击复位，自动记忆（localStorage），支持键盘 ←/→ 微调与触屏拖动
- **静态资源免缓存** —— 后端补 `Cache-Control: no-cache` 等响应头，
  前端加 `?v=` 版本参数；改完前端刷新即生效，不用手动清缓存

### 修复

- **分类区折叠状态错乱** —— `folded` 与 `expanded` 改为互斥，
  展开后底部渐变遮罩不再残留；重渲染时保持用户已选的展开状态；
  按钮文字与实际状态始终同步
- **背景音乐不播放** —— 补回漏抽的内置 `src/bgm.mp3`（11.06 MB）
- **`启动.bat` 双击无反应** —— 批处理文件原为 UTF-8 编码，
  中文被 cmd.exe 按 GBK 读取后乱码，把命令本身也拆坏了。
  改为纯 ASCII + CRLF，并新增英文名入口 `run.py`

### 新增

- **本地运行方式** —— 双击 `启动.bat` 即可，不必装 Docker；
  自动探测可用 Python（以「能 import requests」为标准）
- **影库内存测试工具** —— 定位「追加影库崩溃」真因：
  `Library.load()` 里 `json.load` 的原始 dict 无法释放，
  与解析后对象同时驻留；实测 192.7 MB 影库常驻 845 MB、峰值 ~1,500 MB
- **影库拆分工具** —— 降低单次加载峰值

### 文档

- 仓库精简：删除冗余的 `docker/README.md`、两份专用 compose、
  `本地启动.py`，`版本更新说明` 并入维护手册
- 维护手册修正自检项数（13 → 31）

---

## v1.0.5 — 2026-09-16

### 改进

- **版本号改为构建时注入** —— 由 CI 从 git tag 通过 `--build-arg` 传入
  `LB_VERSION` 环境变量，页面显示的版本与镜像 tag 自动一致，
  不再需要手改代码里的版本号

> 修复上一版遗留问题：拉取 v1.0.4 镜像后，页面仍显示 V1.0.3
> （源码里硬编码的常量没跟着改）。

---

## v1.0.4 — 2026-09-16

### 重大变更

- **镜像改为直接构建 `src/` 源码，不再依赖桌面版 exe**

  此前镜像构建要先把 exe 里的字节码抽出来再加载，本版起直接构建源码。
  构建上下文从 **29.1 MB 降到 0.3 MB**。

### 新增

- `src/` 完整可运行源码（从 exe 字节码逐函数手工还原 +
  `tools/verify_src.py` 机械校验）
- `docker/selfcheck.py` —— 免 Docker 的构建自检（31 项，含真实启动探活）
- `docker-compose.yml` —— 变量驱动，支持 `LB_IMAGE` / `LB_TAG` / `LB_PORT`
- `发布新版本.py` —— 一键发布（自检 → 提交 → 打 tag → 推送）

### 说明

四个源码模块的还原一致率：`pan123_api.py` 100%、`tasks.py` 100%、
`server.py` 73%、`library_store.py` 66%（不一致处多为写法差异，
功能完整；已实测通过启动、加载影库、搜索、导出）。

---

## v1.0.3 — 2026-09-15

### 修复

- **追加影库时闪退** —— 根因是内存被撑爆：程序 `json.load` 一次性全量
  解析影库并常驻内存，加载峰值可达文件体积的 8 倍，追加时内存累加
  导致进程被系统杀掉。
  - 新增影库瘦身工具（按目录/条数拆分，可降峰值 93%）
  - 修复断点续扫的 skipped 计数问题

---

## 版本号机制

| 环境 | 版本号来源 |
|---|---|
| Docker 容器 | 构建时由 git tag 经 `--build-arg APP_VERSION` 注入 `LB_VERSION` |
| 本地直跑源码 | `src/server.py` 里的 `APP_VERSION` 兜底值 |

前端 `src/index.html` 的 `style.css?v=` / `app.js?v=` 参数用于破浏览器
缓存，**不显示在界面上**，发版时手动同步为当前版本号即可。
