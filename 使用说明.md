# 123云盘影库搜索工具 使用说明

一套本地工具，用于管理 123 云盘「秒传链接」影库 JSON：按片名搜索作品、按分类浏览、导出秒传文件、从分享链接提取新影库、并把秒传文件批量导入自己的 123 云盘网盘。

> 想换自己喜欢的背景音乐？把音乐文件改名为 `bgm.mp3`（支持 mp3/flac/ogg/m4a/aac/wav）放进数据目录，删除该文件恢复内置曲目。

## 部署方式

### 方式一：本地电脑直接运行（最简单，不用 Docker）

**Windows：直接双击 `run.py`** 即可，浏览器会自动打开。

> 若双击没反应，在本文件夹的地址栏输入 `cmd` 回车，然后运行：
> `.venv\Scripts\python.exe run.py`，这样能看到完整错误信息。

其他系统 / 命令行：

```bash
# 1) 首次准备（只需一次）
python -m venv .venv
.venv/Scripts/python.exe -m pip install requests     # Windows
# .venv/bin/python -m pip install requests           # macOS / Linux

# 2) 启动
python run.py
```

启动后浏览器会自动打开 `http://127.0.0.1:5890/`。

**常用参数：**

| 参数 | 说明 |
|---|---|
| `python run.py` | 正常启动，自动开浏览器 |
| `python run.py --check` | 只做环境自检，不启动（排错用） |
| `python run.py --no-browser` | 不自动开浏览器 |
| `python run.py --dir D:\影库` | 指定别的数据目录 |

**启动前的环境要求：** Python 3.8+ 与 `requests` 库。
缺依赖时启动会给出明确提示。

> ⚠️ **影库较大时的提示**：本工具加载影库时会占用较多内存
> （实测 192 MB 的影库需 845 MB 内存、13 秒加载）。
> 若启动慢或闪退，先用 `python tools/mem_test.py --append` 测一下内存需求。

#### 文件说明（本地运行相关）

| 文件 | 作用 |
|---|---|
| `run.py` | 启动脚本（双击即可，英文名不受系统编码影响） |
| `.venv/` | 项目专用 Python 环境（含 requests） |

### 方式二：Docker 容器（适合 NAS / 服务器长期运行）

适合 NAS / Linux 服务器长期挂在后台，局域网任意设备用浏览器访问。

```bash
# 拉取现成镜像
docker run -d --name pan123-library \
  -p 5890:5890 \
  -v "$PWD/data:/data" \
  -e TZ=Asia/Shanghai \
  -e LB_BROWSER=1 \
  --restart unless-stopped \
  ghcr.io/sadmion/pan123-library:latest
```

也可以从源码构建（不依赖 GHCR）：

```bash
git clone https://github.com/sadmion/123-.git && cd 123-
mkdir -p data && docker compose up -d --build
```

部署、运维、备份、排错的完整说明见 [`docker/维护手册.md`](docker/维护手册.md)。

### 两种方式的区别

| | 本地运行 | Docker |
|---|---|---|
| 数据目录 | 项目下的 `data/` | 挂载的 `./data`（容器内 `/data`） |
| 访问地址 | `http://127.0.0.1:5890/` | `http://<机器IP>:5890/` |
| 局域网访问 | 默认仅本机 | 可（监听 0.0.0.0） |
| 适用场景 | 自己电脑上用 | NAS / 服务器长期跑 |

## 首次使用

1. **准备数据目录**
   - 本地运行：项目下的 `data/`（不存在会自动创建）
   - Docker：映射的宿主机目录（默认 `./data`）
   所有影库 JSON、导出文件、断点记录、登录凭据都存在这里。

2. **放入影库**：把影库 JSON 文件（如 `影巢官组345.33T资源.123fastlink.json`）
   放进 `秒传文件导入或追加/` 子目录。

   > ⚠️ **重要**：程序会**自动监听**该目录，文件一进去就立刻加载。
   > 如果影库太大（>100 MB），在内存不足的机器上可能导致进程被系统杀掉
   > （表现为"崩溃、无日志"）。参见下方「常见问题」。

3. **打开页面**
   - 本地：`http://127.0.0.1:5890/`（`run.py` 会自动打开浏览器）
   - Docker：`http://<机器IP>:5890/`
   左侧出现「已加载 N 个影库 · N 个文件」即成功。

## 功能速览

- **搜索**：顶栏输入片名关键词（≥2 字）回车。支持片名、目录名、分类名模糊匹配；搜索历史自动记录在右侧。
- **分类浏览**：主区按分类（电影、国产剧集、欧美剧集、日韩剧集、动漫、综艺、纪录……）聚合，可筛选、折叠，多选后合并导出。
- **作品卡片**：显示片名、年份、视频数、体积、文件数；可展开查看文件清单并勾选部分导出。海报自动从 TMDB 拉取。
- **导出**：选中作品或分类后生成标准 `.123fastlink.json` 秒传文件，落在 `./data/秒传文件导出/`。
- **分享提取**：粘贴 123 云盘分享链接（支持带提取码、批量多行），扫描分享目录并生成影库 JSON；支持断点续扫，中途可停止。
- **秒传导入**：把影库里的秒传文件批量导入自己的 123 云盘。支持 **扫码登录 / Token 登录 / 账号密码登录**三种方式，走官方秒传接口，**不消耗下载流量**。
- **背景音乐**：左上角唱片按钮开关（默认关闭）。自定义见本文开头。

## 常用操作

| 想做什么 | 怎么做 |
|---|---|
| 换端口 | compose 里把 `"5890:5890"` 改成 `"8080:5890"`，或 `LB_PORT=8080 docker compose up -d` |
| 更新版本 | `docker compose pull && docker compose up -d` |
| 看日志 | `docker compose logs -f`（也可看 `./data/logs/server.log`） |
| 备份数据 | 整个 `./data/` 目录拷走即可（含登录态、影库、断点、历史） |
| 导入新影库 | 把 JSON 丢进 `./data/秒传文件导入或追加/`，自动加载 |
| 登录状态 | 左上角「秒传导入 · 123云盘」区域，登录后重启容器仍免登录 |

## 常见问题

- **页面打不开**
  - 本地：确认 `run.py` 还在运行（窗口没关）；看窗口里打印的访问地址
  - Docker：`docker compose ps` 确认容器在跑；`netstat -tlnp | grep 5890` 确认端口没被占用；云服务器需在安全组放行
- **启动就闪退 / 放影库进去就崩，且日志无任何错误**
  → **几乎可以确定是内存不足（OOM）**。程序会自动加载目录里的影库，
  数据库大时内存需求约为**体积的 4~8 倍**（实测 192 MB → 845 MB）。
  内存不够时内核直接杀进程，**不产生任何日志**。
  处理见 [`docker/维护手册.md`](docker/维护手册.md) 的「影库太大 / 容器内存不足」，
  或先测一下：`python tools/mem_test.py --append --limit 1024`
- **影库一直不显示**：确认 JSON 放对了目录（`秒传文件导入或追加/`），
  并看日志有没有「加载 xxx 失败」
- **缺依赖 requests**：`pip install requests`；或用项目自带 venv
  （`.venv/Scripts/python.exe`）
- **秒传导入失败**：确认已登录，且目标目录有写入权限。导入进度在界面上可见，
  失败原因写在日志区（最多保留 300 条）

## 安全提醒

本工具**没有账号体系**，能访问端口就能使用其中的 123 云盘登录态。请只在局域网 / 家庭 NAS 使用，不要直接暴露到公网；外网访问请加反向代理认证或走 VPN。

登录凭据保存在数据目录的 `记录数据存放目录【勿动】/.login.json`，已在 `.gitignore` 中排除，请勿提交到公开仓库。
