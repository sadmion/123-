# 123云盘影库搜索工具 V1.0.3 使用说明（分享版）

一套本地工具，用于管理 123 云盘「秒传链接」影库 JSON：按片名搜索作品、按分类浏览、导出秒传文件、从分享链接提取新影库、并把秒传文件批量导入自己的 123 云盘网盘。

> 想换自己喜欢的背景音乐？把音乐文件改名为 `bgm.mp3`（支持 mp3/flac/ogg/m4a/aac/wav）放到 exe 旁边即可，删除该文件恢复内置曲目。

## 文件说明

| 文件 | 用途 |
|---|---|
| `123云盘影库搜索工具.exe` | 桌面版：双击后打开原生窗口（1560×900），需电脑装有 Edge WebView2 运行时（Win10/11 一般自带） |
| `123云盘影库搜索工具浏览器版.exe` | 浏览器版：双击后自动打开默认浏览器操作，无 WebView2 依赖 |
| `docker-compose.yml` + `docker/` | 容器版：适合 NAS / Linux 服务器，`docker compose up -d --build` 后浏览器访问 `5890` 端口，详见 [`docker/README.md`](docker/README.md) |

两个 exe 版本功能完全一致，任选其一即可；容器版与它们也是同一份构建产物，功能一致。

## 首次使用

1. 把影库 JSON 文件（如 `影巢官组345.33T资源.123fastlink.json`）和 exe 放在同一文件夹，或先记好它的位置。
2. 双击 exe，首次启动会弹窗让你**选择数据存放目录**——选一个空间充足、路径不含特殊字符的文件夹（所有影库 JSON、导出文件、断点记录都存在这里）。启动时会自动把 exe 同目录下的 `*.json` 复制进去。
3. 左侧出现「已加载 N 个影库 · N 个文件」即成功。之后想换数据目录，用命令行 `exe路径 --select` 或 `--dir=目录`。

## 功能速览

- **搜索**：顶栏输入片名关键词（≥2 字）回车。支持片名、目录名、分类名模糊匹配；搜索历史自动记录在右侧。
- **分类浏览**：左侧点分类标签筛选；标签可折叠/展开；勾选多个分类后点「合并导出选中分类」。
- **作品卡片**：显示片名、年份、视频数、体积；点「展开文件」查看全部文件并可勾选部分导出；右上角海报来自 TMDB（需能访问 TMDB 的网络环境，无海报自动留白，可在右侧替换 API Key）。
- **导出**：全部生成 `.123fastlink.json` 秒传文件，保存在数据目录的 `秒传文件导出\` 下，可点「打开目录」直达。
- **分享提取**：左侧粘贴 123 云盘分享链接（支持带提取码、支持批量多行），选类型过滤后开始提取，进度实时显示；中途关掉程序下次可断点续扫。
- **秒传导入**：右侧三种登录方式（Bearer Token / 扫码 / 账号密码）登录 123 云盘后，选择要导入的秒传 JSON、填写目标目录即可批量入库。导入走官方秒传接口，不消耗流量，文件在云端已存在时会直接复用。
- **背景音乐**：左上角唱片按钮可开关（默认关闭，不扰人）。内置曲目为《more than words》（羊文学）；想换成自己喜欢的歌，把音乐文件改名为 `bgm.mp3`（也支持 flac/ogg/m4a/aac/wav）放到 exe 旁边即可，删除该文件恢复内置。

## V1.0.3 更新内容

- 秒传导入修复：自动兼容 base62（123fastlink V2）/ hex / base64 三种 etag 格式，秒传接口域名更新
- 扫码登录修复：全程复用网络会话，扫码确认后可正常换取登录凭据
- 登录持久化：账号密码/扫码登录一次后自动记住，重启免登录
- 新增「停止导入」按钮：秒传导入过程中可随时中断，已导入的文件保留
- BGM 更换为《more than words》，并支持自定义音乐文件
- 错误日志：数据目录 `logs\server.log` 记录导入失败等详细原因

## Docker / NAS 部署（推荐给服务器用户）

不想在 Windows 上跑，或想让家里 NAS、Linux 小主机长时间挂着服务。镜像已发布，**无需自己构建**：

```bash
docker compose up -d              # 直接拉取现成镜像并启动
# 浏览器访问 http://<机器IP>:5890/
```

- 想自己编译（改过代码、或想确认构建过程）用 `docker compose up -d --build`。
- 支持 `linux/amd64` 与 `linux/arm64`（NAS、树莓派均可）。
- 所有数据落在项目目录下的 `data/`（影库 JSON、导出文件、登录态、日志），删容器不丢数据。
- 影库 JSON 丢进 `data/秒传文件导入或追加/` 会自动加载；换端口用 `LB_PORT=8080 docker compose up -d`。
- 容器版与桌面版是同一份构建产物，功能完全一致。
- 完整部署文档（镜像地址、权限、常见问题、安全提示）见 [`docker/README.md`](docker/README.md)。

> 首次拉取若报 `unauthorized`：GHCR 镜像默认是私有的，去仓库 `Packages` → `Package settings` 把可见性改成 `Public`。
>
> 安全提醒：该工具没有账号体系，能访问端口就能用你的 123 云盘登录态，**请只在局域网使用，不要直接暴露到公网**。

## 常见问题

- **双击没反应/一闪而过**：用命令行运行 `exe路径` 看报错；或换浏览器版。
- **搜索不到片子**：确认左侧影库已加载（文件数 > 0）；关键词至少 2 个字。
- **海报不显示**：本机无法访问 api.themoviedb.org 所致，不影响其他功能。
- **导入失败**：检查登录是否过期、秒传 JSON 是否为该工具导出的标准格式、目标目录是否有写权限；导入过程有限速保护，大批量请耐心等待。
