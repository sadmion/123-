# 打不开 / 不能运行 —— 排查指南

## 最快的解决办法

```bash
# 在项目目录下执行（需要 Python 3.8+；若没有 Python 见下方"没有 Python 怎么办"）
python 启动修复.py
```

这个脚本会自动创建数据目录、修正配置，然后告诉你重新启动 exe 即可。
**90% 的"双击没反应"都是它修的这个问题。**

---

## 一、最常见的故障：双击 exe 没反应 / 浏览器打不开

### 症状

- 双击 `123云盘影库搜索工具.exe`，任务栏闪一下就没了（或完全没动静）
- 浏览器访问 `http://127.0.0.1:5890/` 提示"无法访问此网站"
- 命令行运行 exe 时看到：**`未选择数据目录, 退出`**

### 原因

这是本工具的一个设计特性（不是 bug）：

程序启动时要读取配置里的**数据目录**：

```
%APPDATA%\123云盘影库搜索工具\data_config.json
   → { "data_root": "某个路径" }
```

如果 `data_root` 指向的目录**不存在**，程序会尝试**弹出一个图形化的目录选择框**让你重新选。
但如果弹窗失败（或程序在无界面环境运行），它就打印 `未选择数据目录, 退出` 并**直接退出**。

**触发场景**（都遇到过）：
- 换电脑、重装系统后，旧的数据目录路径失效
- 把项目文件夹移动/重命名了（配置里存的还是旧路径）
- 数据目录被手动删除或磁盘没挂载
- 从别人那里拷贝来项目，配置指向的是**原作者的路径**

### 解决

**方法 A（推荐）：跑修复脚本**

```bash
python 启动修复.py
```

**方法 B：手动改配置**

1. 在项目目录下建好数据目录：
   ```
   123搜索工具\data\
   └── 秒传文件导入或追加\     ← 把影库 JSON 放这里
   ```

2. 编辑 `%APPDATA%\123云盘影库搜索工具\data_config.json`：
   ```json
   { "data_root": "C:/Users/你的用户名/Documents/123搜索工具/data" }
   ```
   （路径可以用正斜杠 `/`，JSON 里反斜杠要写成 `\\`）

3. 重新双击 exe

**方法 C：删掉配置让它重新问**

删除 `%APPDATA%\123云盘影库搜索工具\data_config.json`，然后**双击 exe 并在弹出的窗口里选择目录**。
（注意：这需要桌面环境；远程/无界面环境请用方法 A 或 B）

### 快速定位配置目录

在文件管理器地址栏粘贴：

```
%APPDATA%\123云盘影库搜索工具
```

---

## 二、其他可能原因

### 端口被占用

症状：exe 启动后立刻退出，日志里有 `Address already in use` 之类。

```bash
# 查谁占用了 5890
netstat -ano | findstr 5890
# 按 PID 结束（把 <PID> 换成上面查到的数字）
taskkill /PID <PID> /F
```

### 缺少 WebView2 运行时（桌面版）

- **桌面版** `123云盘影库搜索工具.exe` 需要 Edge WebView2 运行时（Win10/11 一般自带）
- 如果启动后没窗口，**改用 `123云盘影库搜索工具浏览器版.exe`** —— 它不依赖 WebView2，直接用系统浏览器打开

### 杀毒软件拦截

exe 是 PyInstaller 打包的，部分杀软会误报/静默拦截。把项目目录加入白名单，或临时关闭防护再试。

### 数据目录在移动硬盘/网络盘上

如果 `data_root` 指向移动硬盘或网络映射盘，拔盘后目录就不存在了 → 触发第一节的问题。建议把数据目录放在本机固定磁盘。

---

## 三、看日志定位问题

程序会把错误写进数据目录下的日志：

```
<你的数据目录>\logs\server.log
```

看最后几十行通常就能定位原因。

---

## 四、没有 Python 怎么办

`启动修复.py` 需要 Python。如果没有装，可改用**手动方式**（第一节的方法 B），或者：

- 用记事本打开 `%APPDATA%\123云盘影库搜索工具\data_config.json`，手动改 `data_root`
- 确保该路径对应的文件夹**真实存在**（在文件管理器里能看到）

---

## 五、确认服务是否真的起来了

```bash
# Windows 命令行（cmd 或 PowerShell）
netstat -ano | findstr 5890
```

看到下面这样就是成功了：

```
TCP    127.0.0.1:5890    0.0.0.0:0    LISTENING    <PID>
```

此时打开浏览器访问 **http://127.0.0.1:5890/** 即可。

> 注意：程序默认只监听 `127.0.0.1`，所以**同一局域网的其他设备访问不了**。
> 局域网访问请用 Docker 部署方式（见 `docker/README.md`，容器里已改为监听 `0.0.0.0`）。

---

## 六、还是打不开？

把这两样发出来即可定位：

1. `python 启动修复.py` 的完整输出
2. `<数据目录>\logs\server.log` 的最后 50 行
