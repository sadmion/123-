# 版本更新说明

本文件说明如何在本项目上更新版本或调整功能。

---

## 一、项目形态

本项目**包含完整源码**，可直接修改并构建部署。

| 组成 | 位置 | 可否修改 |
|---|---|---|
| 后端逻辑 | `src/server.py` `src/pan123_api.py` `src/tasks.py` `src/library_store.py` | ✅ 明文 Python，直接改 |
| 前端界面 | `src/index.html` `src/style.css` `src/app.js` | ✅ 明文 JS/CSS，直接改 |
| 容器构建 | `docker/Dockerfile` `docker/entry.py` | ✅ 完全可控 |
| 容器编排 | `docker-compose.yml` `compose-*.yml` | ✅ 完全可控 |
| CI 发布 | `.github/workflows/docker-publish.yml` | ✅ 打 tag 自动构建镜像 |

镜像**直接构建 `src/` 源码**，不依赖任何 Windows 产物——改完代码重建镜像即可生效。

> 历史背景：源码最初是从桌面版 exe 的字节码逐函数手工还原出来的
> （现成反编译工具不支持 Python 3.13）。还原质量由 `tools/verify_src.py`
> 校验，它把源码编译后与原始字节码逐函数比对，抓「写错但不报错」的问题。

---

## 二、更新流程

### 标准流程（一键完成）

```bash
# 1) 改 src/ 下的代码
vim src/library_store.py

# 2) 一键发布：自检 → 提交 → 打 tag → 推送
python 发布新版本.py --version 1.0.6 --note "优化分类聚合性能"
```

这条命令会依次完成：

1. 校验仓库状态（源码是否更新、能否连远程）
2. 跑 `docker/selfcheck.py`（30 项构建自检，不需要 Docker）
3. 确认版本号、检查 tag 是否已存在
4. 提交改动
5. 推送 `main` 并推送 tag → **GitHub Actions 自动构建镜像并推送到 GHCR**

构建进度：https://github.com/sadmion/123-/actions

### 服务器端更新

```bash
# 用现成镜像的服务器（推荐）
docker compose pull && docker compose up -d

# 用本地构建的服务器
git pull && docker compose up -d --build
```

### 只调前端

前端是明文文件，改完同样走上面的一键发布。**不需要重新打包 exe**。

### 只调版本号

版本号由构建时从 git tag 注入（`LB_VERSION` 环境变量），页面显示与镜像 tag
自动保持一致。**不需要手改代码里的版本号**。

若本地直跑源码想改默认版本号，改 `src/server.py` 里的
`APP_VERSION = os.environ.get('LB_VERSION') or 'V1.0.5'` 兜底值即可。

---

## 三、每次更新后的检查清单

- [ ] `python docker/selfcheck.py` 通过（30 项，0 失败）
- [ ] 提交信息写清版本号与主要变更
- [ ] 打 tag（`v*` 格式），触发 CI 构建镜像
- [ ] Actions 页面确认构建为绿色
- [ ] 服务器执行更新命令并访问验证
- [ ] 页面标题旁的版本号与本次 tag 一致

> `发布新版本.py` 已把前 4 步自动化，正常情况只需跑一条命令。

---

## 四、相关文件索引

| 文件 | 用途 |
|---|---|
| `src/server.py` | 入口：HTTP 服务 + API 路由 + 数据目录管理 |
| `src/pan123_api.py` | 123云盘接口（登录、分享解析、秒传上传） |
| `src/tasks.py` | 后台任务（分享提取、秒传导入） |
| `src/library_store.py` | 影库数据层（加载、分类聚合、搜索、导出） |
| `docker/Dockerfile` | 镜像构建（直接构建 src/ 源码） |
| `docker/entry.py` | 容器启动器（改监听地址、屏蔽桌面壳、备数据目录） |
| `docker/selfcheck.py` | 免 Docker 的构建自检（30 项，含启动探活） |
| `tools/verify_src.py` | 源码还原校验器（比对源码与原始字节码） |
| `.github/workflows/docker-publish.yml` | 打 tag 自动构建并推送镜像到 GHCR |
| `.github/workflows/ci-diagnose.yml` | CI 环境自检（排查构建问题用） |
| `docker/维护手册.md` | 部署运维主手册 |
| `docker/README.md` | 部署详细说明 |

---

## 五、注意事项

- **不要提交登录凭据**：`data/记录数据存放目录【勿动】/.login.json` 已在 `.gitignore` 排除，务必保持。
- **不要提交大数据**：`*.123fastlink.json` 影库清单、`data/` 目录同样已排除。
- **镜像名固定为 `pan123-library`**：不要改回仓库名 `123-`（结尾连字符属非法镜像名，会报 `invalid reference format`）。
- **GHCR 包可见性**：如需让其他服务器匿名拉取，需在 Packages → Package settings 中设为 Public。
- **自检失败不要强行发布**：`发布新版本.py` 会在自检不通过时中止，这是有意设计。
