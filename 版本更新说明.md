# 版本更新与功能同步说明

本文件说明如何在本项目上更新版本或调整功能，以及每次更新后需要做什么。

---

## 一、当前项目形态（重要前提）

本项目**只包含 exe 成品与容器化部署文件，不包含 Python 源码**。

| 组成 | 文件 | 可否修改 |
|---|---|---|
| 桌面版主程序 | `123云盘影库搜索工具.exe` | ⚠️ 二进制，无法直接改（见下） |
| 前端界面 | 打包在 exe 内：`index.html` / `style.css` / `app.js` | ✅ 明文 JS/CSS，可改（需重新打进 exe 或走容器覆盖） |
| 后端逻辑 | 打包在 exe 内：`server.pyc` / `pan123_api.pyc` / `tasks.pyc` / `library_store.pyc` | ❌ 仅字节码，难改 |
| 容器化部署 | `docker/` + `docker-compose.yml` + `.github/workflows/` | ✅ 完全可控 |

### 为什么后端改不了

exe 由 **Python 3.13** 打包。反编译回源码需要工具支持该版本字节码，而
`decompyle3` / `uncompyle6` 均明确报错 `Unsupported Python version, 3.13.0`。
这是工具链现状，不是配置问题。**如需改后端逻辑，必须拿到原始源码。**

---

## 二、更新流程

### 场景 A：拿到新版本的 exe（最常见）

适用于：作者发布新版、你在别处打包了新 exe。

```bash
# 1. 覆盖项目里的 exe（文件名保持一致）
#    把新 exe 放到项目根目录，覆盖 123云盘影库搜索工具.exe

# 2. 构建前自检（不需要 Docker，几秒出结果）
python docker/selfcheck.py
#    期望输出：结果：13 项通过，0 项失败

# 3. 提交并推送
git add -A
git commit -m "更新到 V1.0.x：<简述新功能>"
git tag -a v1.0.x -m "123云盘影库搜索工具 V1.0.x"
git push origin main && git push origin v1.0.x

# 4. CI 会自动构建新镜像并推送到 GHCR（约 1~2 分钟）
#    查看：https://github.com/sadmion/123-/actions
```

**服务器端更新**：

```bash
# 用本地构建方式的服务器
git pull && docker compose up -d --build

# 用现成镜像方式的服务器
docker compose pull && docker compose up -d
```

### 场景 B：只调整前端界面

前端 `app.js` / `index.html` / `style.css` 是明文，可直接修改。但要注意：
它们**打包在 exe 内部**，修改后需要重新打包进 exe 才能生效。

- 若走**容器部署**：可以让容器优先加载外置的前端文件（需扩展 `docker/entry.py`）。
- 若走**桌面 exe**：必须重新打包，需要 PyInstaller 与源码环境。

### 场景 C：修改后端逻辑

**必须拿到源码。** 源码到位后建议直接纳入仓库管理：

```
server.py           # 入口：HTTP 服务 + 路由 + 数据目录管理
pan123_api.py       # 123云盘接口（登录、秒传、文件列表）
tasks.py            # 导入/提取任务
library_store.py    # 影库索引与检索
index.html / style.css / app.js   # 前端
```

纳入源码后，`docker/Dockerfile` 可改为直接 COPY 源码（不再需要从 exe 抽字节码），
构建更快、可维护性更好。届时同步流程简化为：

```bash
git add -A && git commit -m "新功能：xxx" && git push
# 服务器：git pull && docker compose up -d --build
```

---

## 三、每次更新后的检查清单

- [ ] `python docker/selfcheck.py` 通过（13 项，0 失败）
- [ ] 提交信息写清版本号与主要变更
- [ ] 打 tag（`v*` 格式），触发 CI 构建镜像
- [ ] Actions 页面确认构建为绿色
- [ ] 服务器执行更新命令并访问验证
- [ ] 若版本号变化，同步更新 `docker-compose.yml` 里的 `image: pan123-library:x.y.z`

---

## 四、相关文件索引

| 文件 | 用途 |
|---|---|
| `docker/Dockerfile` | 镜像构建（构建期从 exe 抽字节码） |
| `docker/entry.py` | 容器启动器（改监听地址、屏蔽桌面壳、备数据目录） |
| `docker/extract_pyinstaller.py` | 从 exe 抽取字节码与前端资源 |
| `docker/selfcheck.py` | 免 Docker 的构建自检 |
| `.github/workflows/docker-publish.yml` | 打 tag 自动构建并推送镜像到 GHCR |
| `.github/workflows/ci-diagnose.yml` | CI 环境自检（排查构建问题用） |
| `docker/README.md` | 部署详细说明 |

---

## 五、注意事项

- **不要提交登录凭据**：`data/记录数据存放目录【勿动】/.login.json` 已在 `.gitignore` 排除，务必保持。
- **不要提交大数据**：`*.123fastlink.json` 影库清单、`data/` 目录同样已排除。
- **镜像名固定为 `pan123-library`**：不要改回仓库名 `123-`（结尾连字符属非法镜像名，会报 `invalid reference format`）。
- **GHCR 包可见性**：如需让其他服务器匿名拉取，需在 Packages → Package settings 中设为 Public。
