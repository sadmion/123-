# 追加影库崩溃 —— 本地复现与真因（2026-09-16 重测）

## 一、结论先行

| 项目 | 结论 |
|---|---|
| **现象** | 把影库文件**放进 `秒传文件导入或追加/` 目录后进程直接消失**（无提示、日志无记录） |
| **直接原因** | 进程内存被系统 OOM 杀掉 |
| **触发方式** | **文件一进目录就自动加载**——不是点按钮触发的（见第三节） |
| **真因** | `Library.load()` 里有一份**完整原始 JSON dict 无法释放**，与解析后的对象**同时驻留** |
| **原诊断报告的说法** | 峰值 = 文件体积 × 8，拆分可降 93% |
| **本次重测的修正** | 拆分只降**常驻**内存的 28%；原报告的 93% 是**峰值**降幅，但**常驻内存由记录总数决定，拆不开** |

---

## 一·五、崩溃的完整触发链路（已实测复现）

**这是最关键的发现**：崩溃**不需要点任何按钮**。

```
你把影库 JSON 拷进  秒传文件导入或追加/
        ↓
LibraryStore.watcher_loop 每 3 秒扫一次目录（library_store.py:328）
        ↓
scan() 发现新文件 → 立即调 lib.load() 全量加载（library_store.py:311）
        ↓
内存冲上峰值 → 超过容器上限 → 内核 SIGKILL
        ↓
进程瞬间消失，日志什么都没有
```

> **同理，容器启动时也会崩**：`LibraryStore.__init__` 直接调用
> `scan(initial=True)`（library_store.py:273），启动即同步加载目录内全部影库。

### 实测复现记录

模拟「程序在运行 → 你把文件拷进去」：

| 时刻 | 动作/状态 | 进程内存 |
|---|---|---|
| 步骤 1 | 空目录启动服务 | 24 MB |
| 步骤 2 | watcher 开始运行 | 24 MB |
| 步骤 3 | 拷入 192.7 MB 影库 | 24 MB |
| 第 2 秒 | watcher 开始加载 | **600 MB** |
| 第 4 秒 | 加载中 | **915 MB** |
| **第 5~6 秒** | **峰值** | **950 MB** |
| 第 7 秒 | 加载完成 | 871 MB |

**峰值 950 MB 出现在加载过程中间。** 容器内存不足时，**在还没加载完就被杀了**——
所以你看不到任何错误，就是"崩了"。

---

## 二、实测数据（本机 16GB，Windows）

### 单库加载

| 文件 | 体积 | 加载后内存 | 峰值增量 | 耗时 | 系数 |
|---|---|---|---|---|---|
| 合并影库_去重_重分类.json | 192.7 MB | **845 MB** | ~950 MB | 12.6 s | **4.4** |
| 影巢官组345.33T资源.json | 31.1 MB | **93 MB** | — | 1.6 s | **3.0** |

> ⚠️ **注意系数不一致（4.4 vs 3.0）**：取决于记录里带多少可选字段
> （`type`/`fileName`/`s3KeyFlag`）。所以**不能纯按体积精确推算**，
> 只能粗筛。要准确数字必须实测。

### 内存里到底有什么（tracemalloc）

```
343.1 MB   json/decoder.py:361        ← 原始 dict！load() 返回后仍存活
151.7 MB   library_store.py:206       ← entry['type'] 字段
 46.7 MB   library_store.py:199       ← entry{'path','etag','size'}
  6.9 MB   library_store.py:256       ← Work.files 追加
  6.7 MB   library_store.py:249       ← works 字典 key
```

**343 MB 的原始 dict 是最大的单块。** 它本该在解析完就被回收。

### 拆分方案对比

| 方案 | 合计体积 | 记录数 | **常驻内存** |
|---|---|---|---|
| 单个大库 | 192.7 MB | 746,686 | **845 MB** |
| 拆成 24 个小库 | 192.8 MB | 746,686 | **632 MB** |

**拆分收益：845 → 632 MB，降 25%。** 不是原报告说的 93%。

> 原报告的 93% 指的是**单次加载峰值**（1495 MB → 110 MB）。
> 但**拆分后 24 个库会全部加载并同时常驻**，所以常驻总量由**记录总数**决定，拆不开。

---

## 三、为什么会崩：小内存环境

本机 16GB 可用 8GB，871 MB 撑得住。**崩溃发生内存受限环境**（NAS / 容器）。

推算：

| 环境可用内存 | 192.7MB 单库能否加载 |
|---|---|
| 8 GB | 可以（871 MB） |
| 2 GB | 危险（峰值 ~1.5 GB） |
| **1 GB** | **必崩** |
| **512 MB**（部分 NAS 容器默认） | **必崩** |

**容器还有个额外因素**：Docker 默认可能限制内存。容器内 OOM 时，进程被 `SIGKILL`，
**没有任何 Python 异常、没有 traceback、日志什么都没写** —— 完全符合"闪退、无记录"。

---

## 四、本地怎么测（不依赖 Docker）

用 `tools/mem_test.py`：

```bash
# 0) 粗筛：不加载，只按体积估算（毫秒级，偏保守）
#    想快速看"我这堆库会不会超标"用这个
python tools/mem_test.py --plan --limit 1024

# 1) 逐个加载（每个加载后释放）
python tools/mem_test.py

# 2) ★ 准确值：全部保持常驻，复现「追加/启动」时的真实内存
python tools/mem_test.py --append --limit 1024

# 3) 对比拆分方案：把拆分目录当小库加载
python tools/mem_test.py --compare ./瘦身输出
```

**判读方法**：
- `--plan` 看「峰值估算」，偏保守，用来快速筛；
- `--append` 看最后一行「当前进程内存」，**这是准确值**。
- 若结果接近或超过目标环境可用内存 → **就是会崩**。

### 关键：找准你的内存上限

```bash
# NAS / 服务器上
free -m                                          # 宿主机可用
docker stats pan123-library --no-stream          # 容器当前占用
docker inspect pan123-library | grep -i memory   # 容器内存限制
```

### 复现「崩溃」而不真崩

用内存上限模拟（Linux / WSL）：

```bash
python -c "
import resource, runpy, sys
resource.setrlimit(resource.RLIMIT_AS, (1024*1024*1024,)*2)
sys.argv = ['mem_test.py', '--append']
runpy.run_path('tools/mem_test.py', run_name='__main__')
"
```

Windows 无 `resource` 模块，改用 `--limit` 参数判断即可。

---

## 五、怎么解决

### 方案 0：先把目录里的库瘦下来（治本于当下）

如果目录里已经躺着大库，**容器一起来就会崩**。先把它们拆小：

```bash
python tools/split_library.py "data/秒传文件导入或追加/合并影库_去重_重分类.json" --info
python tools/split_library.py "data/秒传文件导入或追加/合并影库_去重_重分类.json" --by-dir 50000

# 移走原大库，放入拆分结果
mkdir -p "data/秒传文件导入或追加/_old"
mv "data/秒传文件导入或追加/合并影库_去重_重分类.json" "data/秒传文件导入或追加/_old/"
cp 瘦身输出/*.json "data/秒传文件导入或追加/"
```

### 方案 1：确认容器内存够（最直接）

```yaml
# docker-compose.yml
services:
  pan123-library:
    deploy:
      resources:
        limits:
          memory: 2G        # 至少 2G，4G 更稳
```

先查 NAS 实际可用内存：

```bash
docker stats pan123-library        # 看实时内存占用
free -m                            # 看宿主机可用
docker inspect pan123-library | grep -i memory
```

### 方案 2：拆分大影库 + 分批加载

**拆分能降的是峰值（防 OOM 那一下），不能降常驻总量。**

```bash
# 按分类拆成多个小库（用内联脚本，不依赖已删的 影库瘦身.py）
python tools/split_library.py "data/秒传文件导入或追加/合并影库_去重_重分类.json" \
    --by-dir 50000
```

### 方案 3：改加载实现（根治，需要改源码）

当前实现的问题在 `Library.load()`：
- `data = json.load(f)` 的完整 dict 被 `raw_files` 的子对象引用**钉住**，无法释放
- `entry` 里存了 `type`/`fileName`/`s3KeyFlag` 三个派生字段（占 152 MB）

**优化方向：**

1. **解析后立即切断引用**：把需要的数据取出后 `data = None`
2. **不存冗余字段**：`type` 等可现算，不必每条都存
3. **流式解析**（治本）：用 `ijson` 逐条读，内存不随文件膨胀

> ⚠️ 注意：方案 3 会**改变行为**，违背"1:1 还原官方逻辑"的原则。
> 如果要以不变应万变，优先用**方案 1（加内存）**。

---

## 六、复现步骤（给用户）

```bash
# 1. 本机克隆/进入项目
cd 123搜索工具

# 2. 把影库放进数据目录
ls data/秒传文件导入或追加/

# 3. 测内存（不启动服务，纯加载）
python tools/mem_test.py --append --limit 1024

# 4. 看输出最后一行的「当前进程内存」
#    如果 > 你 NAS 可用内存 → 就是这个原因
```

**关键判读**：如果第 4 步显示 ~870 MB，而你 NAS 容器只给了 512 MB 或 1 GB，
**崩溃原因就确认了**。

---

## 附：为什么日志里什么都没有

OOM 杀进程是**内核行为**，不是程序异常：

| 死法 | 现象 |
|---|---|
| Python 抛异常 | 有 traceback、日志有记录 |
| **OOM killer（SIGKILL）** | **进程瞬间消失，无任何输出** ← 本次情况 |
| 段错误 | 可能留 core dump |

所以「日志里查不到」**本身就是 OOM 的典型特征**。
