# 追加影库崩溃 —— 原因与修复

## 一句话结论

**追加影库时进程闪退，是因为内存被撑爆**：程序加载影库时的内存**峰值可达文件体积的 8 倍**。
**解决办法：把大影库拆成小库**（已实测可将峰值从 1495 MB 降到 110 MB，降幅 93%）。

---

## 一、原因（已实测确认）

程序的加载方式：`json.load` **一次性全量解析**整个影库 → 转成对象常驻内存。
追加新影库时，内存**在新旧数据基础上累加**，超过可用内存后进程被系统杀掉，
表现为**网页点「追加影库」后程序闪退**（无任何错误提示、日志也不记录）。

### 实测数据

以 192.7 MB / 746,686 条的影库为例：

| 指标 | 数值 |
|---|---|
| 文件体积 | 192.7 MB |
| 记录数 | 746,686 |
| 加载后内存 | 535 MB |
| **加载峰值** | **1,495 MB** |
| 加载耗时 | 12.6 秒 |

**峰值 ≈ 文件体积 × 7.8**。所以：

| 影库体积 | 预计加载峰值 | 风险 |
|---|---|---|
| 30 MB | ~230 MB | 安全 |
| 100 MB | ~780 MB | 偏高 |
| **192 MB** | **~1,500 MB** | **危险，易崩溃** |

### 进程现状（问题现场）

| 项目 | 数值 |
|---|---|
| 系统可用内存 | 6.1 GB |
| 进程实测占用 | 1,073 MB |
| 已加载影库 | 2 个（87,037 + 746,686 = 833,723 条） |

---

## 二、修复：把大影库拆小

> **核心原理**：加载峰值正比于**单个文件**的大小。文件越小，峰值越低，越不容易崩溃。

### 步骤 1：查看影库状况

```bash
python 影库瘦身.py "你的影库.json" --info
```

输出示例：

```
  记录数    : 746686
  总体积    : 1644.94 TB
  加载内存  : ≈ 1503 MB（峰值）
  ⚠ 该影库加载峰值较高，建议拆分后再导入

  顶级分组（前 15）:
    有声书      245107 条
    电视剧      204556 条
    动漫        194785 条
    电影         53453 条
    ...
```

### 步骤 2：拆分

```bash
# 按分类拆分，每个子库最多 5 万条（推荐）
python 影库瘦身.py "你的影库.json" --split-by-dir 50000

# 或按条数顺序切块
python 影库瘦身.py "你的影库.json" --split 50000
```

输出到 `瘦身输出/` 目录。以 192.7 MB 影库为例，拆成 **24 个 ≤16 MB 的小库**。

### 步骤 3：导入

```bash
# 1) 先移走原来的超大影库（关键！否则内存仍会叠加）
#    移动到项目外的其他文件夹即可

# 2) 把拆分结果复制进「秒传文件导入或追加」目录
copy 瘦身输出\*.json "你的数据目录\秒传文件导入或追加\"

# 3) 重启程序
```

> **重要**：数据目录是**程序配置里指定的那个**（默认在项目下的 `data\秒传文件导入或追加\`，
> 不是项目根目录的同名文件夹）。不确定的话跑 `python 启动修复.py` 会打印出来。

### 效果（实测）

| 指标 | 拆分前 | 拆分后 | 改善 |
|---|---|---|---|
| 单次加载峰值 | **1,495 MB** | **110 MB** | **↓ 93%** |
| 单次加载内存 | 535 MB | 38~42 MB | ↓ 92% |
| 单库体积 | 192.7 MB | ≤ 16 MB | ↓ 92% |
| 加载耗时 | 12.6 秒 | < 1 秒 | ↓ 95% |

---

## 三、其他瘦身方式

```bash
# 只保留某类内容
python 影库瘦身.py "影库.json" --filter "电影/"

# 随机抽样 30%（快速瘦身，适合只做检索用）
python 影库瘦身.py "影库.json" --sample 0.3

# 组合：先过滤再拆
python 影库瘦身.py "影库.json" --filter "剧集/" --split 50000

# 先预览不写入
python 影库瘦身.py "影库.json" --split 50000 --dry-run
```

---

## 四、日常使用建议

| 建议 | 说明 |
|---|---|
| **单个影库 ≤ 5 万条** | 峰值约 110 MB，安全 |
| **导入前看内存** | `tasklist /FI "PID eq <PID>" /FO LIST`，留足余量 |
| **不要一次导入多个大库** | 内存是累加的 |
| **用完后移走不用的影库** | 减少常驻内存 |
| **数据目录放在 SSD 固定盘** | 避免机械盘/网络盘拖慢加载 |

---

## 五、为什么不能直接「修程序」

程序的内存问题是**架构性**的，根治需要改内部逻辑：

1. `Library.load` 用 `json.load` 一次性解析 → 峰值 1.5 GB
2. 每条记录转成对象常驻 → 全量约 2.2 GB
3. 追加时不卸载旧影库 → 内存持续累加

改进方向（需能编译的完整源码）：

- **流式解析**（`ijson` / 逐行解析）→ 峰值可降到几 MB
- **索引化存储**（只留紧凑索引，详情按需读）
- **LRU 淘汰**（限制常驻库数）
- **紧凑数据结构**（`array`/numpy 替代 `list[dict]`，内存可降 5~10 倍）

当前项目只有 exe（源码为约 81% 覆盖率的反编译还原稿，见 `tools/`），
可用于阅读理解逻辑，但**尚不足以直接编译运行**。因此现阶段采用
「拆分影库」这一外部方案，可完全规避崩溃。

---

## 六、附带发现：两个目录容易混淆

程序生效的目录是**配置里 `data_root` 下的**子目录，不是项目根目录的同名文件夹：

| 路径 | 说明 |
|---|---|
| `<data_root>\秒传文件导入或追加\` | ✅ **程序实际扫描的目录** |
| `<项目目录>\秒传文件导入或追加\` | ❌ 不会生效（早期结构遗留） |

确认 `data_root`：

```bash
python 启动修复.py --check
```

---

*诊断时间：2026-09-15 ｜ 数据来源：实测（tracemalloc + 进程观测 + 字节码执行）*
